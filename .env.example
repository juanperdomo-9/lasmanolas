SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=

SITE_URL=http://127.0.0.1:8000

# Postgres (Neon). Vacío = sigue usando SQLite local, como hasta ahora.
# Formato: postgres://usuario:password@host/nombre_db?sslmode=require
DATABASE_URL=

# Cloudinary (fotos subidas desde /panel/). Vacío = sigue guardando en
# el filesystem local, como hasta ahora. Se copia tal cual del dashboard
# de Cloudinary ("API Environment variable").
# Formato: cloudinary://api_key:api_secret@cloud_name
CLOUDINARY_URL=

# Mercado Pago (Checkout Pro)
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=

# WhatsApp de contacto (formato internacional, sin +, sin espacios)
WHATSAPP_NUMBER=5491100000000

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Las Manolas <no-reply@lasmanolas.com.ar>
